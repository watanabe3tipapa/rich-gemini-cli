[project]
name = "rich-gemini-cli"
version = "0.1.0"
description = "Richフォーマット対応のGemini AI用コマンドラインインターフェース"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "rich>=14.1.0",
    "httpx>=0.27.0",
]
[project.scripts]
gemini-cli = "gemini:main"
